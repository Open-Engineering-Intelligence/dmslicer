# Legacy provenance

## Frozen baseline

- Legacy project: `DMSlicer-AI-mod`
- Legacy path: `D:\Agent\raw\gradient-slicing\legacy-code\Slicer\DMSlicer-AI-mod`
- Legacy branch: `master`
- Legacy baseline commit: `1207c97f88df047b29218c324084a1c8e542f386`

The legacy repository is preserved as a read-only research baseline and historical reference. Its principal implemented concerns are:

- AMF surface-triangle representation;
- AABB/BVH candidate filtering;
- angle, gap, and projected-overlap contact heuristics;
- pair, patch, and component evidence organization;
- `RegionTriangle` and local repair experiments;
- a Streamlit material-role workflow;
- PyVista visualization; and
- manual source, gradient, and isolation semantics.

## Interpretation boundary

The legacy repository is not the new `dmslicer` implementation. Its algorithms may be retained as a research baseline, historical reference, manual/heuristic comparison, and regression evidence.

The legacy system must not be described as implementing STEP/B-rep geometry truth, a volumetric gradient field, a complete slicer, or an industrial G-code pipeline.

Scientific provenance is preserved by this document, the frozen research contract, and the exact baseline commit identifier. The legacy Git DAG and source tree are not imported into this repository.
