# 09B 通用几何预览交接点

状态：**暂停于实现中，尚未完成浏览器验收。**

## 已完成

- 56810 保持冻结，未修改。
- 09B 材料库与材料表单主流程已完成：常见材料、颜色、温度单值/范围、备注、本地 JSON 导入导出。
- 09B 已有 `.dmslicer` 本地导入入口，并已移除依赖 A/G/B 名称的选择逻辑。
- 新增 `dmslicer.display-links.v1` 校验模型，用稳定引用表达输入、interface、patch、剩余部分和融合结果之间的关系。
- 生成了五个不覆盖原始包的关联样例副本：CASE01、C02、U05、S04、P-MULTI。
- Python 现有定向检查：14 项通过。

## 当前未完成

- 56811 尚未重启到 `display-links-001/samples.json`。
- 尚未逐例完成浏览器检查，因此不能宣称圆柱、球面和多补丁全部可用。
- 浏览器测试尚需补齐五例断言：选中 0.6、相邻 0.2、其他 0.1，以及各例 interface/patch/remaining 数量。
- 新导入包若没有 `display_links`，界面应明确显示“未提供已确认关联”；这一降级路径尚需浏览器验收。
- 当前代码中仍有早期被覆盖的 JavaScript 片段，可在功能通过后清理，避免本轮扩大修改范围。

## 下次第一步

1. 用新样例清单重启 56811。
2. 先测 C02、U05、S04、P-MULTI 的单选关联，不先改材料区。
3. 补浏览器自动化断言并运行回归。
4. 通过后再做实际浏览器旋转、选择、融合视图和本地导入复核。
5. 将结果写入本目录的 `checks.json`，再把任务标记完成。

## 停止条件

任何案例仍依赖显示名称、列表顺序或猜测关系时立即停止，不宣称完成；所有关联必须来自包内明确证据或本目录记录的样例映射。
