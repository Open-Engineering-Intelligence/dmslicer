diff --git a/pyproject.toml b/pyproject.toml
index f4a471d..4e4ebf8 100644
--- a/pyproject.toml
+++ b/pyproject.toml
@@ -13,7 +13,7 @@ dependencies = ["jsonschema>=4.23"]
 where = ["src"]
 
 [tool.setuptools.package-data]
-"dmslicer" = ["geometry_import_viewer.html", "viewer_layout.js"]
+"dmslicer" = ["geometry_import_viewer.html", "viewer_layout.js", "workspace_annotations.js", "material_workspace.js"]
 "dmslicer.geometry_contract.schemas" = ["*.json"]
 
 [tool.pytest.ini_options]
diff --git a/src/dmslicer/geometry_case_viewer.py b/src/dmslicer/geometry_case_viewer.py
index 378bbed..91c958d 100644
--- a/src/dmslicer/geometry_case_viewer.py
+++ b/src/dmslicer/geometry_case_viewer.py
@@ -50,7 +50,11 @@ def render_catalog(value, *, import_enabled=False):
     payload = json.dumps(value, ensure_ascii=True, allow_nan=False).replace('<', '\\u003c')
     template = Path(__file__).with_name('geometry_import_viewer.html').read_text(encoding='utf-8')
     layout = Path(__file__).with_name('viewer_layout.js').read_text(encoding='utf-8')
-    return template.replace('__LAYOUT_CODE__', layout).replace('__CATALOG_JSON__', payload).replace('__IMPORT_ENABLED__', 'true' if import_enabled else 'false')
+    annotations = Path(__file__).with_name('workspace_annotations.js').read_text(encoding='utf-8')
+    material_ui = Path(__file__).with_name('material_workspace.js').read_text(encoding='utf-8')
+    return (template.replace('__LAYOUT_CODE__', layout).replace('__ANNOTATION_CODE__', annotations)
+            .replace('__MATERIAL_CODE__', material_ui).replace('__CATALOG_JSON__', payload)
+            .replace('__IMPORT_ENABLED__', 'true' if import_enabled else 'false'))
 
 
 def case01_entry(directory: Path, root: Path):
diff --git a/src/dmslicer/geometry_import_viewer.html b/src/dmslicer/geometry_import_viewer.html
index 863f05e..ac125cd 100644
--- a/src/dmslicer/geometry_import_viewer.html
+++ b/src/dmslicer/geometry_import_viewer.html
@@ -4,20 +4,29 @@
 <title>DM-Slicer 几何结果工作台</title>
 <style>
 *{box-sizing:border-box}body{margin:0;font:14px/1.5 system-ui;color:#243443;background:#f4f6f8;display:grid;grid-template-rows:auto 1fr;height:100vh}header{background:white;padding:14px 22px;border-bottom:1px solid #ccd4dc}h1{font-size:20px;margin:0 0 6px}.toolbar{display:flex;align-items:center;flex-wrap:wrap;gap:12px}button,select,input{font:inherit}button{padding:5px 10px;cursor:pointer}main{display:grid;grid-template-columns:minmax(0,1fr) 340px;min-height:0}#viewport{position:relative;min-height:300px}canvas{width:100%;height:100%;display:block;background:#15212b}#empty{position:absolute;left:10%;right:10%;top:35%;text-align:center;color:#d7e4ec;pointer-events:none}aside{padding:16px;overflow:auto;background:white}p{margin:6px 0}.muted{font-size:12px;color:#607284}#status{color:#805315;overflow-wrap:anywhere}details{border-top:1px solid #dde3e8;padding:9px 0}summary{cursor:pointer;font-weight:600}.entity{display:flex;gap:6px;align-items:center;margin:5px 0}.entity button{flex:1;text-align:left;border:0;background:#f0f4f7;overflow-wrap:anywhere}.entity button[aria-pressed=true]{outline:2px solid #eead45}pre{white-space:pre-wrap;overflow-wrap:anywhere;font-size:11px;max-height:330px;overflow:auto}label{display:block;margin:7px 0}#filter{width:100%;padding:6px}#saved{max-width:270px}[hidden]{display:none!important}@media(max-width:800px){main{grid-template-columns:1fr;grid-template-rows:55vh auto}body{height:auto}aside{overflow:visible}}
-main{overflow:auto}main.evidence-mode{grid-template-columns:1fr}main.evidence-mode aside{max-width:1200px;width:100%;margin:auto}main.evidence-mode pre{max-height:none}.evidence-mode #display-tools{display:none}#viewport{min-width:0}#mode{padding:5px}#inspector details[data-evidence] pre{color:#35485b}</style></head><body>
-<header><h1>DM-Slicer 几何结果工作台</h1><div class="toolbar"><label><strong>导入 DM-Slicer 结果包</strong> <input id="package-file" type="file" accept=".dmslicer"></label><label>视图 <select id="mode"><option value="overlay">整体查看</option><option value="comparison">操作对照</option><option value="evidence">证据查看</option></select></label><button id="fit">适合窗口 / Fit</button><label id="sample-control" hidden>示例包 <select id="samples"><option value="">选择示例…</option></select></label><select id="saved" aria-label="包内案例"></select></div><div id="status" role="status">选择一个 .dmslicer 结果包开始。导入成功不代表几何验证通过。</div></header>
-<main id="workspace"><div id="viewport"><canvas id="scene" aria-label="3D display mesh"></canvas><div id="empty">导入一个 .dmslicer 结果包<br>旋转：拖动 · 缩放：滚轮 · 选择：单击</div></div><aside id="inspector">
-<p><strong>STEP / B-rep 是几何来源</strong></p><p class="muted">网格只用于显示。显隐不执行 union，不表示允许路径穿越。透明度与前后面颜色不判定材料侧。接口补丁可以开放或有界，不能用实体闭合标准判无效。</p>
-<p id="source" class="muted"></p><p id="explanation" class="muted"></p><div id="display-tools"><label>实体透明度 <input id="opacity" type="range" min="0.1" max="1" step="0.05" value="1"></label><label><input id="backfaces" type="checkbox"> 区分网格前后面（双面均显示）</label><label><input id="boundaries" type="checkbox" checked> 显示 B-rep 边界线（穿透显示）</label><p id="boundary-status" class="muted"></p><input id="filter" placeholder="搜索名称或引用"><div id="controls"></div></div><details data-evidence><summary>来源/诊断 · Selection traceability</summary><pre id="trace">尚未选择对象</pre></details><details data-evidence><summary>来源与证据状态</summary><pre id="provenance"></pre></details>
-<details data-evidence open><summary>包内文件与可用状态</summary><div id="contents"></div></details><details data-evidence><summary>已保存的计算 / 验证事实</summary><pre id="facts"></pre></details><p class="muted">本页只读取结果包。源结果可由 FreeCAD / OCCT 生成。STEP 是交换几何；BREP 为当前内核表示。FCStd 和显示缓存都是可选附件。</p></aside></main>
+main{overflow:auto}main.evidence-mode{grid-template-columns:1fr}main.evidence-mode aside{max-width:1200px;width:100%;margin:auto}main.evidence-mode pre{max-height:none}.evidence-mode #display-tools{display:none}#viewport{min-width:0}#mode{padding:5px}#inspector details[data-evidence] pre{color:#35485b}
+body{grid-template-rows:auto auto minmax(0,1fr) auto;height:100vh}main{overflow:auto}aside{padding:16px}nav{display:flex;gap:6px;padding:10px 22px;background:#fff;border-bottom:1px solid #ccd4dc}nav button{border:0;border-radius:5px;background:#edf2f5;padding:10px 18px}nav button[aria-selected=true]{background:#176e68;color:white}button:focus-visible,input:focus-visible,select:focus-visible,textarea:focus-visible,summary:focus-visible{outline:3px solid #d48824;outline-offset:2px}#selection-summary{background:#e9f2f0;padding:8px;overflow-wrap:anywhere}#selection-names{font-size:12px;max-height:75px;overflow:auto;display:block}.batch{display:flex;flex-wrap:wrap;gap:5px}.entity{display:grid;grid-template-columns:auto minmax(0,1fr) auto;gap:5px;border-bottom:1px solid #e2e8ed;padding:9px 0}.entity label{margin:0;font-size:12px}.entity .single-opacity{grid-column:2 / 4;display:flex;gap:5px;align-items:center}.entity-opacity{min-width:60px;width:100%}.entity .object-focus{background:transparent;padding:3px;font-size:13px}.form-mode{display:block;background:#f0f4f6}.form-panel{max-width:1380px;margin:auto;padding:24px}.form-panel h2{margin:0 0 8px}.annotation-row{display:grid;grid-template-columns:minmax(190px,1.15fr) minmax(160px,1fr) minmax(180px,1fr) minmax(160px,1fr);gap:18px;margin:14px 0;background:white;padding:18px;border:1px solid #d6e1e8;border-radius:8px}.annotation-row select{display:block;width:100%;padding:7px;margin:6px 0}.annotation-row .muted,.annotation-identity span{display:block}.annotation-identity{overflow-wrap:anywhere}.annotation-row button{font-size:12px}.display-color{width:60px;height:32px}.notice{background:#e2eeeb;padding:12px;border-left:3px solid #28786f}.annotation-actions{display:flex;gap:12px;align-items:center;flex-wrap:wrap}.annotation-actions label{margin:0}#evidence-drawer{background:white;border-top:1px solid #ccd4dc;padding:12px 22px}#evidence-drawer>summary{color:#235e5b}#evidence-body{max-height:48vh;overflow:auto;padding:12px 4px;display:grid;grid-template-columns:1fr 1fr;gap:16px}#evidence-body pre{max-height:none}#evidence-body h3{margin:0;font-size:14px}#evidence-body section{min-width:0}#material-form{max-width:750px;background:white;border:1px solid #d6e1e8;border-radius:8px;padding:20px}#material-form label{display:block}#material-form input:not([type=color]),#material-form textarea{width:100%;padding:8px;border:1px solid #a6b6c0;border-radius:4px}#material-form textarea{min-height:65px;font:inherit}#material-list{display:flex;gap:10px;flex-wrap:wrap;margin:16px 0}#annotation-status,#library-status{color:#795018;overflow-wrap:anywhere}.muted{overflow-wrap:anywhere}#geometry-toolbar{display:contents}#selection-facts{padding:8px;background:#f1f5f7}#opacity{width:100%}
+@media(max-width:1000px){.annotation-row{grid-template-columns:1fr 1fr}}@media(max-width:800px){body{height:auto;min-height:100vh;display:block}nav{padding:8px;overflow:auto}nav button{white-space:nowrap;padding:8px 12px}main{grid-template-rows:55vh auto}main.form-mode{display:block}.form-panel{padding:16px}.annotation-row{grid-template-columns:1fr 1fr;gap:12px}#evidence-body{grid-template-columns:1fr}#evidence-drawer{padding:12px}header{padding:12px}.toolbar{gap:6px}input[type=file]{max-width:260px}}@media(max-width:520px){.annotation-row{grid-template-columns:1fr}nav{gap:2px}nav button{font-size:12px}#material-form{padding:12px}}
+</style></head><body>
+<header><h1>DM-Slicer 几何结果工作台</h1><div class="toolbar"><label><strong>导入 DM-Slicer 结果包</strong> <input id="package-file" type="file" accept=".dmslicer"></label><span id="geometry-toolbar"><label>视图 <select id="mode"><option value="overlay">整体查看</option><option value="comparison">操作对照</option></select></label><button id="fit">适合窗口 / Fit</button></span><label id="sample-control" hidden>示例包 <select id="samples"><option value="">选择示例…</option></select></label><select id="saved" aria-label="包内案例"></select></div><div id="status" role="status">选择一个 .dmslicer 结果包开始。导入成功不代表几何验证通过。</div></header>
+<nav role="tablist" aria-label="工作区"><button role="tab" aria-selected="true" aria-controls="viewport" data-tab="geometry">几何查看</button><button role="tab" aria-selected="false" aria-controls="annotations-panel" data-tab="annotations" tabindex="-1">材料与语义配置</button><button role="tab" aria-selected="false" aria-controls="library-panel" data-tab="library" tabindex="-1">材料库</button></nav>
+<main id="workspace"><div id="viewport"><canvas id="scene" aria-label="3D display mesh"></canvas><div id="empty">导入一个 .dmslicer 结果包<br>旋转：拖动 · 缩放：滚轮 · 单击：单选对象</div></div><aside id="inspector">
+<p><strong>STEP / B-rep 是几何来源</strong></p><p class="muted">网格只用于显示；颜色、显隐与透明度不判定几何关系或材料侧。</p>
+<div id="display-tools"><div class="batch"><button id="select-all">全选</button><button id="select-none">全不选</button><button id="select-invert">反选</button><button id="show-selected">显示所选</button><button id="hide-selected">隐藏所选</button></div><div id="selection-summary" role="status"></div><label>所选透明度 <output id="opacity-state"></output><input id="opacity" type="range" min="0" max="100" step="1" value="0"></label><p class="muted">0% 不透明 · 100% 全透明。选择操作覆盖全部对象；筛选只改变列表。</p><label><input id="backfaces" type="checkbox"> 区分网格前后面（临时诊断色，不改材料）</label><label><input id="boundaries" type="checkbox" checked> 显示 B-rep 边界线（穿透显示）</label><input id="filter" aria-label="搜索对象" placeholder="搜索名称或引用"><div id="controls"></div></div><div id="selection-facts" class="muted">单击名称查看简洁摘要并单选；复选框增减选择。</div></aside>
+<section id="annotations-panel" class="form-panel" role="tabpanel" hidden><h2>材料与语义配置</h2><p id="activation-state" class="notice">仅人工配置准备 · MaterialRegion / InterfaceSourceBoundary / 体材料场：未激活。Source 与 Gradient 不创建几何或语义实体。</p><div class="annotation-actions"><button id="save-workspace">保存到此浏览器</button><button id="export-workspace">导出配置 JSON</button><label>导入配置 <input id="import-workspace" type="file" accept=".json"></label></div><p id="annotation-status" role="status">未载入对象</p><p class="muted">配置与几何包分别保存。本地缓存不是备份；导出 JSON 可跨浏览器或端口恢复。颜色默认来自材料，显示覆盖单独记录。</p><p id="annotation-empty">先打开一个结果包以配置对象。</p><div id="annotation-rows"></div></section>
+<section id="library-panel" class="form-panel" role="tabpanel" hidden><h2>材料库</h2><p>材料名称与颜色仅描述材料；不会推断对象语义。扩展属性保存为人工数据，不运行材料计算。</p><p id="library-version"></p><div class="annotation-actions"><button id="new-material">新增材料</button><button id="save-library">保存材料库到此浏览器</button><button id="export-library">导出材料库 JSON</button><button id="return-object" hidden>返回原对象</button></div><p id="library-status" role="status"></p><div id="material-list"></div><form id="material-form"><label>稳定材料键（编辑时不变）<input id="material-id" required pattern="[a-zA-Z][a-zA-Z0-9._-]{0,79}" placeholder="polymer-a"></label><label>材料名称<input id="material-name" required maxlength="200"></label><label>材料颜色<input id="material-color" type="color" value="#498b92"></label><label>说明<textarea id="material-description" maxlength="4000"></textarea></label><label>扩展属性（JSON 对象；数值建议携带单位）<textarea id="material-properties">{}</textarea></label><button type="submit">保存材料</button><p class="muted">保存材料更新草稿；保存材料库写入独立浏览器存储。工作区保存会携带本次库版本快照。</p></form></section></main>
+<details id="evidence-drawer"><summary>证据与溯源</summary><div id="evidence-body"><section><h3>对象来源 / 稳定引用</h3><pre id="trace">尚未聚焦对象</pre><p id="source"></p><p id="explanation"></p><p id="boundary-status"></p></section><section><h3>完整来源记录与输入容差</h3><pre id="provenance"></pre></section><section><h3>包内资产链与可用状态</h3><div id="contents"></div></section><section><h3>已保存的计算 / 验证事实</h3><pre id="facts"></pre></section><section><h3>人工配置 / 材料库版本 / 决策历史</h3><pre id="annotation-trace"></pre></section><section><h3>验证边界</h3><p>本次不运行几何验证。文件 hash 仅证明字节完整性。STEP / B-rep 为几何来源；显示缓存和人工配置均不是几何真相。体材料场未激活。</p></section></div></details>
 <script id="catalog-data" type="application/json">__CATALOG_JSON__</script>
 <script id="viewer-code">
 'use strict';
 (() => {
 __LAYOUT_CODE__
+__ANNOTATION_CODE__
 const importEnabled=__IMPORT_ENABLED__, $=id=>document.getElementById(id), canvas=$('scene'), ctx=canvas.getContext('2d');
 let catalog=JSON.parse($('catalog-data').textContent), current=null, entities=[], selected=null, az=-.72, elevation=.44, zoom=1, center=[0,0,0], drag=null, triangles=[];
-const visibility=new Map(), collapsed=new Map();
+let display=WorkspaceAnnotations.display([]);
+const collapsed=new Map();
+__MATERIAL_CODE__
 let layout=makePanelLayout([],false,1,1);
 const comparing=()=>$('mode').value==='comparison';
 function check(value){
@@ -32,9 +41,35 @@ function check(value){
 }
 function status(text){$('status').textContent=text}
 function install(value){check(value);catalog=value;$('saved').replaceChildren();for(const c of catalog.cases){const o=document.createElement('option');o.value=c.case_id;o.textContent=c.label;$('saved').append(o)}$('saved').hidden=catalog.cases.length<2;if(catalog.cases.length)openCase(catalog.cases[0]);}
-function openCase(c){current=c;entities=c.scene.entities;selected=null;visibility.clear();entities.forEach(e=>visibility.set(e.entity_ref,e.default_visible!==false));$('trace').textContent='尚未选择对象';$('source').textContent=c.label+' · '+(c.provenance.semantic_analysis==='NO_MATERIAL_REGION_BINDING'?'未提供材料区域绑定；这些是导入几何对象。':c.provenance.semantic_analysis||'语义状态见来源记录');$('provenance').textContent=JSON.stringify(c.provenance,null,2);$('explanation').textContent=c.provenance.operation_explanation||'';$('boundary-status').textContent=entities.some(e=>e.mesh.boundary_polylines?.length)?'边界线来自 B-rep wire/edge 离散采样；不从三角网格推断边界环。':'此来源未提供 B-rep 边界线。';$('empty').hidden=entities.length>0;$('empty').textContent=entities.length?'':'包已载入；未提供显示缓存。请阅读右侧计算结果与文件清单。当前查看平台不运行几何转换。';$('contents').replaceChildren();for(const asset of c.provenance.package_contents||[]){const p=document.createElement('p');p.className='muted';p.textContent=asset.path+' · '+asset.role+' · '+asset.status;$('contents').append(p)}$('facts').textContent=JSON.stringify(c.provenance.computed_results||{state:'NOT_SUPPLIED'},null,2);renderControls();fit();draw();}
-function renderControls(){const query=$('filter').value.toLowerCase();$('controls').replaceChildren();for(const [kind,title] of (entities.some(e=>e.scene_role)?[['input','导入几何对象 / Inputs'],['corrected','校正几何 / Corrected'],['interface_group','接口关联 / Interfaces'],['common_interface','公共接口补丁 / Patches'],['remaining','剩余分区 / Remaining'],['fused_result','融合结果 / Fused']]:[['MODEL','导入几何对象'],['REGION','导入几何对象 / Geometry'],['INTERFACE','接口 / Interfaces'],['PATCH','接口补丁 / Patches']])){const items=entities.filter(e=>kind==='interface_group'?e.scene_role==='common_interface'&&e.entity_kind==='INTERFACE':kind==='common_interface'?e.scene_role==='common_interface'&&e.entity_kind!=='INTERFACE':(e.scene_role||e.entity_kind)===kind);if(!items.length&&current?.provenance.role_states?.[kind]!=='NOT_PRODUCED_IN_SUPPLIED_EVIDENCE')continue;const d=document.createElement('details'),s=document.createElement('summary');d.open=!collapsed.get(kind);d.ontoggle=()=>collapsed.set(kind,!d.open);s.textContent=title+' ('+items.length+')'+(current?.provenance.role_states?.[kind]==='NOT_PRODUCED_IN_SUPPLIED_EVIDENCE'?' · 本证据未产生':'');d.append(s);for(const [name,state] of (items.length?[['全部显示',true],['全部隐藏',false]]:[])){const b=document.createElement('button');b.textContent=name;b.onclick=()=>{items.forEach(e=>visibility.set(e.entity_ref,state));renderControls();draw()};d.append(b)}for(const e of items.filter(e=>(e.display_name+' '+e.entity_ref).toLowerCase().includes(query))){const row=document.createElement('div'),v=document.createElement('input'),b=document.createElement('button');row.className='entity';v.type='checkbox';v.checked=visibility.get(e.entity_ref);v.setAttribute('aria-label','Visibility '+e.display_name);v.onchange=()=>{visibility.set(e.entity_ref,v.checked);draw()};b.textContent=e.display_name;b.setAttribute('aria-pressed',String(selected===e));b.onclick=()=>select(e);row.append(v,b);d.append(row)}$('controls').append(d)}}
-function select(e){selected=e;$('trace').textContent=JSON.stringify({display_name:e.display_name,entity_ref:e.entity_ref,identity_status:e.identity_status,source:e.source||e.reference_provenance,mesh_provenance:e.mesh.provenance,linear_deflection_mm:e.mesh.linear_deflection_mm,geometry_validation:'NOT_PERFORMED_BY_VIEWER',semantic_analysis:current.provenance.semantic_analysis||'SEE_SOURCE'},null,2);renderControls();draw()}
+function openCase(c){current=c;entities=c.scene.entities;selected=null;display=WorkspaceAnnotations.display(entities);$('evidence-drawer').open=false;prepareAnnotation(c);$('trace').textContent='尚未选择对象';$('source').textContent=c.label+' · '+(c.provenance.semantic_analysis==='NO_MATERIAL_REGION_BINDING'?'未提供材料区域绑定；这些是导入几何对象。':c.provenance.semantic_analysis||'语义状态见来源记录');$('provenance').textContent=JSON.stringify(c.provenance,null,2);$('explanation').textContent=c.provenance.operation_explanation||'';$('boundary-status').textContent=entities.some(e=>e.mesh.boundary_polylines?.length)?'边界线来自 B-rep wire/edge 离散采样；不从三角网格推断边界环。':'此来源未提供 B-rep 边界线。';$('empty').hidden=entities.length>0;$('empty').textContent=entities.length?'':'包已载入；未提供显示缓存。请阅读右侧计算结果与文件清单。当前查看平台不运行几何转换。';$('contents').replaceChildren();for(const asset of c.provenance.package_contents||[]){const p=document.createElement('p');p.className='muted';p.textContent=asset.path+' · '+asset.role+' · '+asset.status;$('contents').append(p)}$('facts').textContent=JSON.stringify(c.provenance.computed_results||{state:'NOT_SUPPLIED'},null,2);renderControls();fit();draw();}
+function updateSelectionSummary(){
+ const chosen=entities.filter(e=>display.selected.has(e.entity_ref)),values=new Set(chosen.map(e=>display.items.get(e.entity_ref).transparency));
+ $('selection-summary').replaceChildren(document.createTextNode('已选择 '+chosen.length+' / '+entities.length));const names=make('span',chosen.map(e=>e.display_name).join('、')||'空选择集','muted');names.id='selection-names';$('selection-summary').append(names);
+ $('opacity').disabled=!chosen.length;$('show-selected').disabled=!chosen.length;$('hide-selected').disabled=!chosen.length;
+ $('opacity-state').textContent=!chosen.length?'无选择':values.size>1?'混合（拖动统一所选）':[...values][0]+'%';
+ if(values.size===1)$('opacity').value=[...values][0];
+ $('opacity').setAttribute('aria-valuetext',$('opacity-state').textContent);
+ if(selected){const o=annotation?.objects.find(o=>o.entity_ref===selected.entity_ref),m=annotation?.material_library.materials.find(m=>m.id===o?.material_id);$('selection-facts').textContent=selected.display_name+' · '+WorkspaceAnnotations.geometryRole(selected)+' · '+(o?.semantic_role||'Unassigned')+'（未激活） · 材料：'+(m?.name||'未分配')}
+ else $('selection-facts').textContent='单击名称查看简洁摘要并单选；复选框增减选择。';
+}
+function renderControls(){
+ const query=$('filter').value.toLowerCase();$('controls').replaceChildren();
+ for(const kind of ['Input','Corrected geometry','Interface','Patch','Remaining geometry','Fused output']){
+  const items=entities.filter(e=>WorkspaceAnnotations.geometryRole(e)===kind);if(!items.length)continue;
+  const d=make('details'),s=make('summary',kind+' ('+items.length+')');d.open=!collapsed.get(kind);d.ontoggle=()=>collapsed.set(kind,!d.open);d.append(s);
+  for(const e of items.filter(e=>(e.display_name+' '+e.entity_ref).toLowerCase().includes(query))){
+   const state=display.items.get(e.entity_ref),row=make('div',undefined,'entity');row.dataset.kind=e.entity_kind;row.dataset.ref=e.entity_ref;
+   const choose=make('input',undefined,'select-object');choose.type='checkbox';choose.checked=display.selected.has(e.entity_ref);choose.setAttribute('aria-label','选择 '+e.display_name);choose.onchange=()=>{if(choose.checked)display.selected.add(e.entity_ref);else display.selected.delete(e.entity_ref);updateSelectionSummary();draw()};
+   const focus=make('button',e.display_name,'object-focus');focus.setAttribute('aria-pressed',String(selected===e));focus.onclick=()=>select(e);
+   const v=make('input',undefined,'visibility');v.type='checkbox';v.checked=state.visible;v.setAttribute('aria-label','显示 '+e.display_name);v.onchange=()=>{state.visible=v.checked;draw()};const vl=make('label');vl.append(v,document.createTextNode('显示'));
+   const opacity=make('input',undefined,'entity-opacity');opacity.type='range';opacity.min=0;opacity.max=100;opacity.step=1;opacity.value=state.transparency;opacity.setAttribute('aria-label','透明度 '+e.display_name);const out=make('output',state.transparency+'%');const ol=make('label',undefined,'single-opacity');ol.append(document.createTextNode('透明度'),opacity,out);opacity.oninput=()=>{state.transparency=Number(opacity.value);out.textContent=opacity.value+'%';updateSelectionSummary();draw()};
+   row.append(choose,focus,vl,ol);d.append(row);
+  }$('controls').append(d);
+ }
+ if(current?.provenance.role_states?.fused_result==='NOT_PRODUCED_IN_SUPPLIED_EVIDENCE')$('controls').append(make('p','Fused output · 本证据未产生','muted'));
+ updateSelectionSummary();
+}
+function select(e){selected=e;display.selected.clear();display.selected.add(e.entity_ref);$('trace').textContent=JSON.stringify({display_name:e.display_name,entity_ref:e.entity_ref,identity_status:e.identity_status,source:e.source,reference_provenance:e.reference_provenance,mesh_provenance:e.mesh.provenance,linear_deflection_mm:e.mesh.linear_deflection_mm,geometry_validation:'NOT_PERFORMED_BY_VIEWER'},null,2);renderControls();draw()}
 function fit(){
  layout=makePanelLayout(entities,comparing(),canvas.width,canvas.height);
  const lo=[Infinity,Infinity,Infinity],hi=[-Infinity,-Infinity,-Infinity];
@@ -47,32 +82,36 @@ function view(p,e){const panel=layout.byRef.get(e.entity_ref)||layout.panels[0],
  return [panel.x+panel.w/2+rx*zoom,panel.y+panel.h/2+(comparing()?14:0)-(Math.cos(elevation)*z-Math.sin(elevation)*ry)*zoom,Math.sin(elevation)*z+Math.cos(elevation)*ry]}
 function draw(){
  ctx.clearRect(0,0,canvas.width,canvas.height);triangles=[];const colors=['#55b6d4','#83cfa2','#cb91e1','#efbf66'];
- entities.forEach((e,i)=>{if(!visibility.get(e.entity_ref))return;for(const t of e.mesh.triangles){const p=t.map(j=>view(e.mesh.positions[j],e));triangles.push({e,p,panel:layout.byRef.get(e.entity_ref),color:e.entity_kind==='PATCH'?'#edb951':colors[i%colors.length],depth:p.reduce((a,v)=>a+v[2],0)/3})}});
+ entities.forEach((e,i)=>{if(!display.items.get(e.entity_ref).visible)return;for(const t of e.mesh.triangles){const p=t.map(j=>view(e.mesh.positions[j],e));triangles.push({e,p,panel:layout.byRef.get(e.entity_ref),color:WorkspaceAnnotations.color(annotation,e.entity_ref),depth:p.reduce((a,v)=>a+v[2],0)/3})}});
  triangles.sort((a,b)=>a.depth-b.depth);
  for(const panel of layout.panels){
   ctx.save();ctx.beginPath();ctx.rect(panel.x,panel.y+(comparing()?35:0),panel.w,panel.h-(comparing()?35:0));ctx.clip();
   for(const t of triangles.filter(t=>t.panel===panel)){const [a,b,c]=t.p,back=(b[0]-a[0])*(c[1]-a[1])-(b[1]-a[1])*(c[0]-a[0])>0;
-   ctx.globalAlpha=Number($('opacity').value);ctx.fillStyle=shade($('backfaces').checked&&back?'#bc7f91':t.color,t.p);ctx.beginPath();ctx.moveTo(a[0],a[1]);ctx.lineTo(b[0],b[1]);ctx.lineTo(c[0],c[1]);ctx.closePath();ctx.fill();if(selected===t.e){ctx.strokeStyle='#ffdc86';ctx.lineWidth=.5;ctx.stroke()}}
+   ctx.globalAlpha=1-display.items.get(t.e.entity_ref).transparency/100;ctx.fillStyle=shade($('backfaces').checked&&back?'#bc7f91':t.color,t.p);ctx.beginPath();ctx.moveTo(a[0],a[1]);ctx.lineTo(b[0],b[1]);ctx.lineTo(c[0],c[1]);ctx.closePath();ctx.fill();if(display.selected.has(t.e.entity_ref)){ctx.strokeStyle='#ffdc86';ctx.lineWidth=.5;ctx.stroke()}}
   ctx.globalAlpha=1;
-  if($('boundaries').checked){ctx.strokeStyle='#b5e5f0';ctx.lineWidth=1;for(const e of panel.items.filter(e=>visibility.get(e.entity_ref)))for(const line of e.mesh.boundary_polylines||[]){ctx.beginPath();line.forEach((p,i)=>{const v=view(p,e);if(i)ctx.lineTo(v[0],v[1]);else ctx.moveTo(v[0],v[1])});ctx.stroke()}}
+  if($('boundaries').checked){ctx.strokeStyle='#b5e5f0';ctx.lineWidth=1;for(const e of panel.items.filter(e=>display.items.get(e.entity_ref).visible))for(const line of e.mesh.boundary_polylines||[]){ctx.globalAlpha=1-display.items.get(e.entity_ref).transparency/100;ctx.beginPath();line.forEach((p,i)=>{const v=view(p,e);if(i)ctx.lineTo(v[0],v[1]);else ctx.moveTo(v[0],v[1])});ctx.stroke()}}
   ctx.restore();
-  if(comparing()){ctx.fillStyle='#dce9f2';ctx.font='14px system-ui';ctx.fillText(panel.title,panel.x+12,panel.y+24,panel.w-24);ctx.strokeStyle='#354754';ctx.strokeRect(panel.x,panel.y,panel.w,panel.h);if(panel.items.length&&!panel.items.some(e=>visibility.get(e.entity_ref))){ctx.fillStyle='#a3b3bf';ctx.fillText('已隐藏 · 可在对象面板中显示',panel.x+16,panel.y+panel.h/2,panel.w-32)}}
+  if(comparing()){ctx.fillStyle='#dce9f2';ctx.font='14px system-ui';ctx.fillText(panel.title,panel.x+12,panel.y+24,panel.w-24);ctx.strokeStyle='#354754';ctx.strokeRect(panel.x,panel.y,panel.w,panel.h);if(panel.items.length&&!panel.items.some(e=>display.items.get(e.entity_ref).visible)){ctx.fillStyle='#a3b3bf';ctx.fillText('已隐藏 · 可在对象面板中显示',panel.x+16,panel.y+panel.h/2,panel.w-32)}}
  }
  canvas.dataset.panelCount=String(layout.panels.length);
 }
 function shade(color,p){const u=[(p[1][0]-p[0][0])/zoom,(p[1][1]-p[0][1])/zoom,p[1][2]-p[0][2]],v=[(p[2][0]-p[0][0])/zoom,(p[2][1]-p[0][1])/zoom,p[2][2]-p[0][2]],n=[u[1]*v[2]-u[2]*v[1],u[2]*v[0]-u[0]*v[2],u[0]*v[1]-u[1]*v[0]],m=Math.hypot(...n)||1,light=.35+.65*Math.abs((n[0]*-.3+n[1]*-.4+n[2]*.866)/m);return 'rgb('+[1,3,5].map(i=>Math.round(parseInt(color.slice(i,i+2),16)*light)).join(',')+')'}
 function hitTest(x,y){let hit=null,depth=-Infinity;for(const t of triangles){const r=t.panel;if(x<r.x||x>r.x+r.w||y<r.y+(comparing()?35:0)||y>r.y+r.h)continue;const [a,b,c]=t.p,d=(b[1]-c[1])*(a[0]-c[0])+(c[0]-b[0])*(a[1]-c[1]);if(!d)continue;const u=((b[1]-c[1])*(x-c[0])+(c[0]-b[0])*(y-c[1]))/d,v=((c[1]-a[1])*(x-c[0])+(a[0]-c[0])*(y-c[1]))/d,z=u*a[2]+v*b[2]+(1-u-v)*c[2];if(u>=0&&v>=0&&u+v<=1&&z>depth){hit=t.e;depth=z}}return hit}
 canvas.onpointerdown=e=>{drag={x:e.clientX,y:e.clientY,moved:0};canvas.setPointerCapture(e.pointerId)};canvas.onpointermove=e=>{if(!drag)return;const dx=e.clientX-drag.x,dy=e.clientY-drag.y;drag.moved+=Math.abs(dx)+Math.abs(dy);az+=dx*.01;elevation=Math.max(-1.55,Math.min(1.55,elevation+dy*.01));drag.x=e.clientX;drag.y=e.clientY;draw()};canvas.onpointerup=e=>{if(drag&&drag.moved<5){const b=canvas.getBoundingClientRect(),hit=hitTest((e.clientX-b.left)*canvas.width/b.width,(e.clientY-b.top)*canvas.height/b.height);if(hit)select(hit)}drag=null};canvas.onpointercancel=()=>drag=null;canvas.addEventListener('wheel',e=>{e.preventDefault();zoom*=e.deltaY>0?.9:1.1;draw()},{passive:false});
-$('mode').onchange=()=>{$('workspace').classList.toggle('evidence-mode',$('mode').value==='evidence');$('viewport').hidden=$('mode').value==='evidence';$('fit').disabled=$('mode').value==='evidence';if($('mode').value==='evidence')document.querySelectorAll('[data-evidence]').forEach(d=>d.open=true);fit()};
-$('filter').oninput=renderControls;$('fit').onclick=fit;for(const id of ['opacity','backfaces','boundaries'])$(id).oninput=draw;$('saved').onchange=()=>openCase(catalog.cases.find(c=>c.case_id===$('saved').value));
+$('mode').onchange=fit;
+$('filter').oninput=renderControls;$('fit').onclick=fit;for(const id of ['backfaces','boundaries'])$(id).oninput=draw;
+$('opacity').oninput=()=>{WorkspaceAnnotations.transparency(display,Number($('opacity').value));renderControls();draw()};
+for(const mode of ['all','none','invert'])$('select-'+mode).onclick=()=>{WorkspaceAnnotations.select(display,mode);renderControls();draw()};
+for(const [id,value] of [['show-selected',true],['hide-selected',false]])$(id).onclick=()=>{WorkspaceAnnotations.show(display,value);renderControls();draw()};
+$('saved').onchange=()=>{if(annotationDirty||libraryDirty){status('有未保存修改，请先保存工作区配置和材料库。');$('saved').value=current.case_id;return}openCase(catalog.cases.find(c=>c.case_id===$('saved').value))};
 $('package-file').disabled=!importEnabled;
 if(!importEnabled)status('离线预览。正常导入结果包请运行 start_geometry_viewer.ps1。');
-async function loadPackage(file){if(file.size>50*1024*1024)throw Error('Package limit: 50 MiB');status('正在打开结果包…');const r=await fetch('/package',{method:'POST',headers:{'Content-Type':'application/octet-stream'},body:file});const value=await r.json();if(!r.ok)throw Error(value.error||'Package import failed');install(value);status('已打开 '+file.name+'；文件完整性检查与几何验证分开。'+(entities.length?'':'当前为无显示缓存的结果阅读模式。'));}
+async function loadPackage(file){if(annotationDirty||libraryDirty)throw Error('有未保存修改，请先保存工作区配置和材料库');if(file.size>50*1024*1024)throw Error('Package limit: 50 MiB');status('正在打开结果包…');const r=await fetch('/package',{method:'POST',headers:{'Content-Type':'application/octet-stream'},body:file});const value=await r.json();if(!r.ok)throw Error(value.error||'Package import failed');install(value);status('已打开 '+file.name+'；文件完整性检查与几何验证分开。'+(entities.length?'':'当前为无显示缓存的结果阅读模式。'));}
 async function loadSamples(){if(!importEnabled)return;try{const r=await fetch('/samples');if(!r.ok)return;const list=await r.json();for(const sample of list){const o=document.createElement('option');o.value=sample.key;o.textContent=sample.label;$('samples').append(o)}$('sample-control').hidden=!list.length}catch{}}
 $('samples').onchange=async()=>{if(!$('samples').value)return;try{const r=await fetch('/sample/'+encodeURIComponent($('samples').value));if(!r.ok)throw Error('示例包不可用');await loadPackage(new File([await r.blob()],$('samples').selectedOptions[0].textContent+'.dmslicer'))}catch(e){status(e.message)}finally{$('samples').value=''}};
 loadSamples();
 $('package-file').onchange=async e=>{try{if(e.target.files[0])await loadPackage(e.target.files[0])}catch(err){status('结果包导入失败：'+err.message+'。原有视图保留。')}finally{e.target.value=''}};
 new ResizeObserver(()=>{canvas.width=Math.max(1,canvas.clientWidth);canvas.height=Math.max(1,canvas.clientHeight);fit()}).observe(canvas);
-install(catalog);document.documentElement.dataset.ready='true';
+initializeMaterialUI();install(catalog);document.documentElement.dataset.ready='true';
 })();
 </script></body></html>
